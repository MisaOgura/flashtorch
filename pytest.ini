[pytest]
addopts =
    -s
    -v
    -l
    --cov torchscope.saliency
    --cov torchscope.utils
    --cov torchscope.utils.imagenet
    --cov-report html

filterwarnings =
    error
    ignore:numpy.dtype size changed
    ignore:numpy.ufunc size changed
    ignore:numpy.ndarray size changed
